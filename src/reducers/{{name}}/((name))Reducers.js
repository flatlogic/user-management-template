import list from 'reducers/((name))/((name))ListReducers';
import form from 'reducers/((name))/((name))FormReducers';
import { combineReducers } from 'redux';

export default combineReducers({
  list,
  form,
});
