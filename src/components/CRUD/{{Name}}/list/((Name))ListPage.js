import React, { Component } from 'react';
import ((Name))ListTable from 'components/CRUD/((Name))/list/((Name))ListTable';

class ((Name))ListPage extends Component {
  render() {
    return (
    	<div>
          <((Name))ListTable />
      	</div>
    );
  }
}

export default ((Name))ListPage;
