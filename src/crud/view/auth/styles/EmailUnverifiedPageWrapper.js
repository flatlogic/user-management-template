import styled from 'styled-components';
import Wrapper from 'crud/view/auth/styles/Wrapper';

const EmailUnverifiedPageWrapper = styled(Wrapper)`
  background-image: url(/images/emailUnverified.jpg);
`;

export default EmailUnverifiedPageWrapper;
